// Stub for better-auth/plugins — not needed in test context
export const organization = jest.fn(() => ({}));
export const bearer = jest.fn(() => ({}));
export const admin = jest.fn(() => ({}));
export const magicLink = jest.fn(() => ({}));
export const multiSession = jest.fn(() => ({}));
export const emailOTP = jest.fn(() => ({}));
