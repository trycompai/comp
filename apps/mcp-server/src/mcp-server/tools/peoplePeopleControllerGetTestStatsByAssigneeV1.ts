/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { peoplePeopleControllerGetTestStatsByAssigneeV1 } from "../../funcs/peoplePeopleControllerGetTestStatsByAssigneeV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$peoplePeopleControllerGetTestStatsByAssigneeV1:
  ToolDefinition = {
    name: "get-test-stats-by-assignee",
    description: `Get integration test statistics grouped by assignee

Get integration test statistics grouped by assignee in Comp AI. Invite and manage workforce members, training status, device compliance, email preferences, and employee evidence records.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] = await peoplePeopleControllerGetTestStatsByAssigneeV1(
        client,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
