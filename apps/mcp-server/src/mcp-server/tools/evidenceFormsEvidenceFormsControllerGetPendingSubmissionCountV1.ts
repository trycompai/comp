/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evidenceFormsEvidenceFormsControllerGetPendingSubmissionCountV1 } from "../../funcs/evidenceFormsEvidenceFormsControllerGetPendingSubmissionCountV1.js";
import { EvidenceFormsControllerGetPendingSubmissionCountV1Request$zodSchema } from "../../models/evidenceformscontrollergetpendingsubmissioncountv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EvidenceFormsControllerGetPendingSubmissionCountV1Request$zodSchema
    .optional(),
};

export const tool$evidenceFormsEvidenceFormsControllerGetPendingSubmissionCountV1:
  ToolDefinition<typeof args> = {
    name: "get-pending-submission-count",
    description: `Get pending submission count for current user

Get pending submission count for current user in Comp AI. Collect, review, upload, and export structured evidence submissions for compliance tasks and document requirements.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await evidenceFormsEvidenceFormsControllerGetPendingSubmissionCountV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
