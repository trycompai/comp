/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evidenceFormsEvidenceFormsControllerGetFormSettingsV1 } from "../../funcs/evidenceFormsEvidenceFormsControllerGetFormSettingsV1.js";
import { EvidenceFormsControllerGetFormSettingsV1Request$zodSchema } from "../../models/evidenceformscontrollergetformsettingsv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EvidenceFormsControllerGetFormSettingsV1Request$zodSchema.optional(),
};

export const tool$evidenceFormsEvidenceFormsControllerGetFormSettingsV1:
  ToolDefinition<typeof args> = {
    name: "get-form-settings",
    description: `Get document relevance settings

Get document relevance settings in Comp AI. Collect, review, upload, and export structured evidence submissions for compliance tasks and document requirements.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await evidenceFormsEvidenceFormsControllerGetFormSettingsV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
