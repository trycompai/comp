/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commentsCommentsControllerCreateCommentV1 } from "../../funcs/commentsCommentsControllerCreateCommentV1.js";
import { CreateCommentDto$zodSchema } from "../../models/createcommentdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateCommentDto$zodSchema,
};

export const tool$commentsCommentsControllerCreateCommentV1: ToolDefinition<
  typeof args
> = {
  name: "create-comment",
  description: `Create a new comment

Create a new comment in Comp AI. Create and manage collaboration comments on compliance entities such as tasks, policies, risks, vendors, and findings.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await commentsCommentsControllerCreateCommentV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
