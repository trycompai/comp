/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsChecksControllerListConnectionChecksV1 } from "../../funcs/integrationsChecksControllerListConnectionChecksV1.js";
import { ChecksControllerListConnectionChecksV1Request$zodSchema } from "../../models/checkscontrollerlistconnectionchecksv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ChecksControllerListConnectionChecksV1Request$zodSchema,
};

export const tool$integrationsChecksControllerListConnectionChecksV1:
  ToolDefinition<typeof args> = {
    name: "list-connection-checks",
    description: `List checks for a connection

List checks for a connection in Comp AI. Connect vendor systems, configure OAuth apps, run compliance checks, sync employees, manage variables, and collect automated evidence.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await integrationsChecksControllerListConnectionChecksV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
