/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { soaSOAControllerGetSetupV1 } from "../../funcs/soaSOAControllerGetSetupV1.js";
import { EnsureSOASetupDto$zodSchema } from "../../models/ensuresoasetupdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EnsureSOASetupDto$zodSchema,
};

export const tool$soaSOAControllerGetSetupV1: ToolDefinition<typeof args> = {
  name: "get-setup",
  description: `Read SOA configuration and document without creating either

Read SOA configuration and document without creating either in Comp AI. Create, auto-fill, review, approve, and export ISO 27001 Statement of Applicability documents.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await soaSOAControllerGetSetupV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
