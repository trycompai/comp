/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contextContextControllerCreateContextV1 } from "../../funcs/contextContextControllerCreateContextV1.js";
import { CreateContextDto$zodSchema } from "../../models/createcontextdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateContextDto$zodSchema.describe(`Context entry data`),
};

export const tool$contextContextControllerCreateContextV1: ToolDefinition<
  typeof args
> = {
  name: "create-context",
  description: `Create a new context entry

Creates a new context entry for the authenticated organization. All required fields must be provided.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await contextContextControllerCreateContextV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
