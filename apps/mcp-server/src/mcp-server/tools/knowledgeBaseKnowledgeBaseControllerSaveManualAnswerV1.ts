/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeBaseKnowledgeBaseControllerSaveManualAnswerV1 } from "../../funcs/knowledgeBaseKnowledgeBaseControllerSaveManualAnswerV1.js";
import { SaveManualAnswerDto$zodSchema } from "../../models/savemanualanswerdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SaveManualAnswerDto$zodSchema,
};

export const tool$knowledgeBaseKnowledgeBaseControllerSaveManualAnswerV1:
  ToolDefinition<typeof args> = {
    name: "save-manual-answer",
    description: `Save reusable manual answer

Save or update a reusable manual answer for security questionnaires that need approved, consistent response language.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await knowledgeBaseKnowledgeBaseControllerSaveManualAnswerV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
