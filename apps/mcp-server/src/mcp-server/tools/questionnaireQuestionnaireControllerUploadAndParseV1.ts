/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { questionnaireQuestionnaireControllerUploadAndParseV1 } from "../../funcs/questionnaireQuestionnaireControllerUploadAndParseV1.js";
import { UploadAndParseDto$zodSchema } from "../../models/uploadandparsedto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UploadAndParseDto$zodSchema,
};

export const tool$questionnaireQuestionnaireControllerUploadAndParseV1:
  ToolDefinition<typeof args> = {
    name: "upload-and-parse",
    description: `Start questionnaire parsing

Upload a questionnaire payload and start asynchronous parsing, returning a run ID for real-time progress tracking.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await questionnaireQuestionnaireControllerUploadAndParseV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
