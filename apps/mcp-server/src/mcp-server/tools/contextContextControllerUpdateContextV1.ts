/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contextContextControllerUpdateContextV1 } from "../../funcs/contextContextControllerUpdateContextV1.js";
import { ContextControllerUpdateContextV1Request$zodSchema } from "../../models/contextcontrollerupdatecontextv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ContextControllerUpdateContextV1Request$zodSchema,
};

export const tool$contextContextControllerUpdateContextV1: ToolDefinition<
  typeof args
> = {
  name: "update-context",
  description: `Update organization context

Update an organization context entry so approved business details stay current for evidence and questionnaire automation.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await contextContextControllerUpdateContextV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
