/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeBaseKnowledgeBaseControllerCreateRunTokenV1 } from "../../funcs/knowledgeBaseKnowledgeBaseControllerCreateRunTokenV1.js";
import { KnowledgeBaseControllerCreateRunTokenV1Request$zodSchema } from "../../models/knowledgebasecontrollercreateruntokenv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: KnowledgeBaseControllerCreateRunTokenV1Request$zodSchema,
};

export const tool$knowledgeBaseKnowledgeBaseControllerCreateRunTokenV1:
  ToolDefinition<typeof args> = {
    name: "create-run-token",
    description: `Create a public access token for a run

Create a public access token for a run in Comp AI. Upload source documents, process them for retrieval, and manage reusable manual answers that power questionnaires and AI policy workflows.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await knowledgeBaseKnowledgeBaseControllerCreateRunTokenV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
