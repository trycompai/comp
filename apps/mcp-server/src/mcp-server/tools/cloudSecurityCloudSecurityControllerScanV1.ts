/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { cloudSecurityCloudSecurityControllerScanV1 } from "../../funcs/cloudSecurityCloudSecurityControllerScanV1.js";
import { CloudSecurityControllerScanV1Request$zodSchema } from "../../models/cloudsecuritycontrollerscanv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CloudSecurityControllerScanV1Request$zodSchema,
};

export const tool$cloudSecurityCloudSecurityControllerScanV1: ToolDefinition<
  typeof args
> = {
  name: "scan",
  description: `Run cloud security scan

Trigger a cloud security scan for a connected AWS, Azure, or GCP account and collect findings for compliance remediation.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await cloudSecurityCloudSecurityControllerScanV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
