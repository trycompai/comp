/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsSyncControllerSetDeviceSyncProviderV1 } from "../../funcs/integrationsSyncControllerSetDeviceSyncProviderV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$integrationsSyncControllerSetDeviceSyncProviderV1:
  ToolDefinition = {
    name: "set-device-sync-provider",
    description: `Set the device sync provider

Set the device sync provider in Comp AI. Connect vendor systems, configure OAuth apps, run compliance checks, sync employees, manage variables, and collect automated evidence.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] = await integrationsSyncControllerSetDeviceSyncProviderV1(
        client,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
