/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeBaseKnowledgeBaseControllerGetViewUrlV1 } from "../../funcs/knowledgeBaseKnowledgeBaseControllerGetViewUrlV1.js";
import { KnowledgeBaseControllerGetViewUrlV1Request$zodSchema } from "../../models/knowledgebasecontrollergetviewurlv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: KnowledgeBaseControllerGetViewUrlV1Request$zodSchema,
};

export const tool$knowledgeBaseKnowledgeBaseControllerGetViewUrlV1:
  ToolDefinition<typeof args> = {
    name: "get-view-url",
    description: `Get a signed view URL for a document

Get a signed view URL for a document in Comp AI. Upload source documents, process them for retrieval, and manage reusable manual answers that power questionnaires and AI policy workflows.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await knowledgeBaseKnowledgeBaseControllerGetViewUrlV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
