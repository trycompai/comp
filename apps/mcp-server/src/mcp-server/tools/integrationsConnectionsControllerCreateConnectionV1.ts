/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsConnectionsControllerCreateConnectionV1 } from "../../funcs/integrationsConnectionsControllerCreateConnectionV1.js";
import { CreateConnectionDto$zodSchema } from "../../models/createconnectiondto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateConnectionDto$zodSchema,
};

export const tool$integrationsConnectionsControllerCreateConnectionV1:
  ToolDefinition<typeof args> = {
    name: "create-connection",
    description: `Create integration connection

Create an integration connection so Comp AI can collect evidence, run checks, or sync data from a connected provider.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await integrationsConnectionsControllerCreateConnectionV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
