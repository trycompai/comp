/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trustAccessTrustAccessControllerPreviewNdaV1 } from "../../funcs/trustAccessTrustAccessControllerPreviewNdaV1.js";
import { TrustAccessControllerPreviewNdaV1Request$zodSchema } from "../../models/trustaccesscontrollerpreviewndav1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TrustAccessControllerPreviewNdaV1Request$zodSchema,
};

export const tool$trustAccessTrustAccessControllerPreviewNdaV1: ToolDefinition<
  typeof args
> = {
  name: "preview-nda",
  description: `Preview Trust Access NDA

Generate a preview NDA PDF for a Trust Access request before the reviewer signs and receives access.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await trustAccessTrustAccessControllerPreviewNdaV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
