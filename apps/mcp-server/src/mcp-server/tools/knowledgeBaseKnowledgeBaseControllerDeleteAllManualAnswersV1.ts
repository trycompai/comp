/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeBaseKnowledgeBaseControllerDeleteAllManualAnswersV1 } from "../../funcs/knowledgeBaseKnowledgeBaseControllerDeleteAllManualAnswersV1.js";
import { DeleteAllManualAnswersDto$zodSchema } from "../../models/deleteallmanualanswersdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteAllManualAnswersDto$zodSchema,
};

export const tool$knowledgeBaseKnowledgeBaseControllerDeleteAllManualAnswersV1:
  ToolDefinition<typeof args> = {
    name: "delete-all-manual-answers",
    description: `Delete all manual answers for an organization

Delete all manual answers for an organization in Comp AI. Upload source documents, process them for retrieval, and manage reusable manual answers that power questionnaires and AI policy workflows.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await knowledgeBaseKnowledgeBaseControllerDeleteAllManualAnswersV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
