/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { soaSOAControllerSaveAnswerV1 } from "../../funcs/soaSOAControllerSaveAnswerV1.js";
import { SaveSOAAnswerDto$zodSchema } from "../../models/savesoaanswerdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SaveSOAAnswerDto$zodSchema,
};

export const tool$soaSOAControllerSaveAnswerV1: ToolDefinition<typeof args> = {
  name: "soa-save-answer",
  description: `Save a SOA answer

Save a SOA answer in Comp AI. Create, auto-fill, review, approve, and export ISO 27001 Statement of Applicability documents.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await soaSOAControllerSaveAnswerV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
