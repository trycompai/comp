/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { cloudSecurityCloudSecurityControllerRevokeExceptionV1 } from "../../funcs/cloudSecurityCloudSecurityControllerRevokeExceptionV1.js";
import { CloudSecurityControllerRevokeExceptionV1Request$zodSchema } from "../../models/cloudsecuritycontrollerrevokeexceptionv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CloudSecurityControllerRevokeExceptionV1Request$zodSchema,
};

export const tool$cloudSecurityCloudSecurityControllerRevokeExceptionV1:
  ToolDefinition<typeof args> = {
    name: "revoke-exception",
    description: `Revoke an exception, reopening the finding

Revoke an exception, reopening the finding in Comp AI. Run AWS, Azure, and GCP cloud security scans, detect enabled services, review findings, and connect cloud posture results to compliance work.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await cloudSecurityCloudSecurityControllerRevokeExceptionV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
