/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { soaSOAControllerCreateDocumentV1 } from "../../funcs/soaSOAControllerCreateDocumentV1.js";
import { CreateSOADocumentDto$zodSchema } from "../../models/createsoadocumentdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateSOADocumentDto$zodSchema,
};

export const tool$soaSOAControllerCreateDocumentV1: ToolDefinition<
  typeof args
> = {
  name: "create-document",
  description: `Create a new SOA document

Create a new SOA document in Comp AI. Create, auto-fill, review, approve, and export ISO 27001 Statement of Applicability documents.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await soaSOAControllerCreateDocumentV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
