/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksTasksControllerUpdateTasksStatusV1 } from "../../funcs/tasksTasksControllerUpdateTasksStatusV1.js";
import { TasksControllerUpdateTasksStatusV1Request$zodSchema } from "../../models/taskscontrollerupdatetasksstatusv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TasksControllerUpdateTasksStatusV1Request$zodSchema,
};

export const tool$tasksTasksControllerUpdateTasksStatusV1: ToolDefinition<
  typeof args
> = {
  name: "update-tasks-status",
  description: `Update status for multiple tasks

Update status for multiple tasks in Comp AI. Manage compliance task lifecycle, assignments, review approvals, evidence uploads, policy links, and activity history.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksTasksControllerUpdateTasksStatusV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
