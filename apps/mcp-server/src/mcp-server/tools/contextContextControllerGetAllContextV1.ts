/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contextContextControllerGetAllContextV1 } from "../../funcs/contextContextControllerGetAllContextV1.js";
import { ContextControllerGetAllContextV1Request$zodSchema } from "../../models/contextcontrollergetallcontextv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ContextControllerGetAllContextV1Request$zodSchema.optional(),
};

export const tool$contextContextControllerGetAllContextV1: ToolDefinition<
  typeof args
> = {
  name: "get-all-context",
  description: `List organization context

List organization context entries used as approved source material for evidence, questionnaires, policies, and AI workflows.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await contextContextControllerGetAllContextV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
