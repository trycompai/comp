/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { devicesDevicesControllerDeleteDeviceV1 } from "../../funcs/devicesDevicesControllerDeleteDeviceV1.js";
import { DevicesControllerDeleteDeviceV1Request$zodSchema } from "../../models/devicescontrollerdeletedevicev1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DevicesControllerDeleteDeviceV1Request$zodSchema,
};

export const tool$devicesDevicesControllerDeleteDeviceV1: ToolDefinition<
  typeof args
> = {
  name: "delete-device",
  description: `Delete device

Delete device in Comp AI. Read and manage employee device inventory and Fleet compliance data used for endpoint security controls.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await devicesDevicesControllerDeleteDeviceV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
