/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { soaSOAControllerApproveDocumentV1 } from "../../funcs/soaSOAControllerApproveDocumentV1.js";
import { ApproveSOADocumentDto$zodSchema } from "../../models/approvesoadocumentdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ApproveSOADocumentDto$zodSchema,
};

export const tool$soaSOAControllerApproveDocumentV1: ToolDefinition<
  typeof args
> = {
  name: "approve-document",
  description: `Approve a SOA document

Approve a SOA document in Comp AI. Create, auto-fill, review, approve, and export ISO 27001 Statement of Applicability documents.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await soaSOAControllerApproveDocumentV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
