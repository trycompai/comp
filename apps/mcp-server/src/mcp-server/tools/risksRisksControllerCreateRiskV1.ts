/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { risksRisksControllerCreateRiskV1 } from "../../funcs/risksRisksControllerCreateRiskV1.js";
import { CreateRiskDto$zodSchema } from "../../models/createriskdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateRiskDto$zodSchema.describe(`Risk creation data`),
};

export const tool$risksRisksControllerCreateRiskV1: ToolDefinition<
  typeof args
> = {
  name: "create-risk",
  description: `Create organization risk

Create a risk record with ownership and context so compliance teams can track mitigation and remediation work.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await risksRisksControllerCreateRiskV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
