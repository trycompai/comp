/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeBaseKnowledgeBaseControllerUploadDocumentV1 } from "../../funcs/knowledgeBaseKnowledgeBaseControllerUploadDocumentV1.js";
import { UploadDocumentDto$zodSchema } from "../../models/uploaddocumentdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UploadDocumentDto$zodSchema,
};

export const tool$knowledgeBaseKnowledgeBaseControllerUploadDocumentV1:
  ToolDefinition<typeof args> = {
    name: "upload-document",
    description: `Upload knowledge base document

Upload supporting documentation so Comp AI can process approved source material for questionnaire answers and policy workflows.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await knowledgeBaseKnowledgeBaseControllerUploadDocumentV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
