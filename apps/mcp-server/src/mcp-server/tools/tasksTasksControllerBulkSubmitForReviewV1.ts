/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksTasksControllerBulkSubmitForReviewV1 } from "../../funcs/tasksTasksControllerBulkSubmitForReviewV1.js";
import { TasksControllerBulkSubmitForReviewV1Request$zodSchema } from "../../models/taskscontrollerbulksubmitforreviewv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TasksControllerBulkSubmitForReviewV1Request$zodSchema,
};

export const tool$tasksTasksControllerBulkSubmitForReviewV1: ToolDefinition<
  typeof args
> = {
  name: "bulk-submit-for-review",
  description: `Bulk submit tasks for review

Bulk submit tasks for review in Comp AI. Manage compliance task lifecycle, assignments, review approvals, evidence uploads, policy links, and activity history.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksTasksControllerBulkSubmitForReviewV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
