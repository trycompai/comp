/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeBaseKnowledgeBaseControllerListDocumentsV1 } from "../../funcs/knowledgeBaseKnowledgeBaseControllerListDocumentsV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$knowledgeBaseKnowledgeBaseControllerListDocumentsV1:
  ToolDefinition = {
    name: "list-documents",
    description: `List knowledge base documents

List uploaded knowledge base documents that Comp AI can use as approved source material for answers, policies, and reviews.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] =
        await knowledgeBaseKnowledgeBaseControllerListDocumentsV1(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
