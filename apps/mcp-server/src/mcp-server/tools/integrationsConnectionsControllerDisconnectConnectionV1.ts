/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsConnectionsControllerDisconnectConnectionV1 } from "../../funcs/integrationsConnectionsControllerDisconnectConnectionV1.js";
import { ConnectionsControllerDisconnectConnectionV1Request$zodSchema } from "../../models/connectionscontrollerdisconnectconnectionv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ConnectionsControllerDisconnectConnectionV1Request$zodSchema,
};

export const tool$integrationsConnectionsControllerDisconnectConnectionV1:
  ToolDefinition<typeof args> = {
    name: "disconnect-connection",
    description: `Disconnect an integration

Disconnect an integration in Comp AI. Connect vendor systems, configure OAuth apps, run compliance checks, sync employees, manage variables, and collect automated evidence.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await integrationsConnectionsControllerDisconnectConnectionV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
