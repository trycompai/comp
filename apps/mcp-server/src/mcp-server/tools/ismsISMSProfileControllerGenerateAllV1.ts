/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ismsISMSProfileControllerGenerateAllV1 } from "../../funcs/ismsISMSProfileControllerGenerateAllV1.js";
import { GenerateAllDto$zodSchema } from "../../models/generatealldto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GenerateAllDto$zodSchema,
};

export const tool$ismsISMSProfileControllerGenerateAllV1: ToolDefinition<
  typeof args
> = {
  name: "generate-all",
  description: `Ensure and regenerate all ISMS documents

Ensure and regenerate all ISMS documents in Comp AI.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await ismsISMSProfileControllerGenerateAllV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
