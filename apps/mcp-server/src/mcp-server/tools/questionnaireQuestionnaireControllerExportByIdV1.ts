/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { questionnaireQuestionnaireControllerExportByIdV1 } from "../../funcs/questionnaireQuestionnaireControllerExportByIdV1.js";
import { ExportByIdDto$zodSchema } from "../../models/exportbyiddto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ExportByIdDto$zodSchema,
};

export const tool$questionnaireQuestionnaireControllerExportByIdV1:
  ToolDefinition<typeof args> = {
    name: "export-by-id",
    description: `Export a security questionnaire

Export a saved security questionnaire response package as PDF, CSV, or XLSX for customer and vendor security reviews.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await questionnaireQuestionnaireControllerExportByIdV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
