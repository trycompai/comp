/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsConnectionsControllerListConnectionsV1 } from "../../funcs/integrationsConnectionsControllerListConnectionsV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$integrationsConnectionsControllerListConnectionsV1:
  ToolDefinition = {
    name: "list-connections",
    description: `List integration connections

List integration connections in Comp AI. Connect vendor systems, configure OAuth apps, run compliance checks, sync employees, manage variables, and collect automated evidence.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] = await integrationsConnectionsControllerListConnectionsV1(
        client,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
