/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trainingTrainingControllerGetCompletionsV1 } from "../../funcs/trainingTrainingControllerGetCompletionsV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$trainingTrainingControllerGetCompletionsV1: ToolDefinition = {
  name: "get-completions",
  description: `List training completions

List security awareness and HIPAA training completion records for workforce compliance tracking and audit evidence.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  tool: async (client, ctx) => {
    const [result] = await trainingTrainingControllerGetCompletionsV1(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
