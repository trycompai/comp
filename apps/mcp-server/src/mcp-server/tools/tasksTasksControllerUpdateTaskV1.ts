/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksTasksControllerUpdateTaskV1 } from "../../funcs/tasksTasksControllerUpdateTaskV1.js";
import { TasksControllerUpdateTaskV1Request$zodSchema } from "../../models/taskscontrollerupdatetaskv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TasksControllerUpdateTaskV1Request$zodSchema,
};

export const tool$tasksTasksControllerUpdateTaskV1: ToolDefinition<
  typeof args
> = {
  name: "update-task",
  description: `Update a task

Update a task in Comp AI. Manage compliance task lifecycle, assignments, review approvals, evidence uploads, policy links, and activity history.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksTasksControllerUpdateTaskV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
