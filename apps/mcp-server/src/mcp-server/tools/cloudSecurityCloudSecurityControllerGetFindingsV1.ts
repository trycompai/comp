/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { cloudSecurityCloudSecurityControllerGetFindingsV1 } from "../../funcs/cloudSecurityCloudSecurityControllerGetFindingsV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$cloudSecurityCloudSecurityControllerGetFindingsV1:
  ToolDefinition = {
    name: "get-findings",
    description: `List cloud security findings

List cloud security findings discovered by scans so teams can prioritize remediation before issues become audit findings.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] = await cloudSecurityCloudSecurityControllerGetFindingsV1(
        client,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
