/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { cloudSecurityCloudSecurityControllerUpdateAwsScanModeV1 } from "../../funcs/cloudSecurityCloudSecurityControllerUpdateAwsScanModeV1.js";
import { CloudSecurityControllerUpdateAwsScanModeV1Request$zodSchema } from "../../models/cloudsecuritycontrollerupdateawsscanmodev1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CloudSecurityControllerUpdateAwsScanModeV1Request$zodSchema,
};

export const tool$cloudSecurityCloudSecurityControllerUpdateAwsScanModeV1:
  ToolDefinition<typeof args> = {
    name: "update-aws-scan-mode",
    description:
      `Switch the AWS scan engine for a connection (Comp AI scanners ↔ Security Hub)

Accepts session, API key, or service token auth. For API key / service token callers without an explicit user attribution, the action is attributed to the org's owner.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await cloudSecurityCloudSecurityControllerUpdateAwsScanModeV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
