/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deviceAgentDeviceAgentControllerGenerateAuthCodeV1 } from "../../funcs/deviceAgentDeviceAgentControllerGenerateAuthCodeV1.js";
import { AuthCodeDto$zodSchema } from "../../models/authcodedto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AuthCodeDto$zodSchema,
};

export const tool$deviceAgentDeviceAgentControllerGenerateAuthCodeV1:
  ToolDefinition<typeof args> = {
    name: "generate-auth-code",
    description: `Create a device-agent auth code

Create a device-agent auth code in Comp AI. Register employee devices, submit device compliance check-ins, download agent builds, and manage endpoint security status.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await deviceAgentDeviceAgentControllerGenerateAuthCodeV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
