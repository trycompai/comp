/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksTasksControllerRejectTaskV1 } from "../../funcs/tasksTasksControllerRejectTaskV1.js";
import { TasksControllerRejectTaskV1Request$zodSchema } from "../../models/taskscontrollerrejecttaskv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TasksControllerRejectTaskV1Request$zodSchema,
};

export const tool$tasksTasksControllerRejectTaskV1: ToolDefinition<
  typeof args
> = {
  name: "reject-task",
  description: `Reject a task review

Reject a task that is in review. Only the assigned approver can reject. Reverts status to the previous status and creates an audit comment.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksTasksControllerRejectTaskV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
