/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { peoplePeopleControllerCreateMemberV1 } from "../../funcs/peoplePeopleControllerCreateMemberV1.js";
import { CreatePeopleDto$zodSchema } from "../../models/createpeopledto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreatePeopleDto$zodSchema.describe(`Member creation data`),
};

export const tool$peoplePeopleControllerCreateMemberV1: ToolDefinition<
  typeof args
> = {
  name: "create-member",
  description: `Create a new member

Adds a new member to the authenticated organization. The user must already exist in the system.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await peoplePeopleControllerCreateMemberV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
