/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commentsCommentsControllerUpdateCommentV1 } from "../../funcs/commentsCommentsControllerUpdateCommentV1.js";
import { CommentsControllerUpdateCommentV1Request$zodSchema } from "../../models/commentscontrollerupdatecommentv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CommentsControllerUpdateCommentV1Request$zodSchema,
};

export const tool$commentsCommentsControllerUpdateCommentV1: ToolDefinition<
  typeof args
> = {
  name: "update-comment",
  description: `Update a comment

Update a comment in Comp AI. Create and manage collaboration comments on compliance entities such as tasks, policies, risks, vendors, and findings.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await commentsCommentsControllerUpdateCommentV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
