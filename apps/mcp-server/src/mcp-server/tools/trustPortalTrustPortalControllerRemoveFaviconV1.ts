/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trustPortalTrustPortalControllerRemoveFaviconV1 } from "../../funcs/trustPortalTrustPortalControllerRemoveFaviconV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$trustPortalTrustPortalControllerRemoveFaviconV1:
  ToolDefinition = {
    name: "remove-favicon",
    description: `Remove the trust portal favicon

Remove the trust portal favicon in Comp AI. Configure the live Trust Center, custom domain, public overview, FAQs, compliance resources, documents, links, and vendor disclosures.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] = await trustPortalTrustPortalControllerRemoveFaviconV1(
        client,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
