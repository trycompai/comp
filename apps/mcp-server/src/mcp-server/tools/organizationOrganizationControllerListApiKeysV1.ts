/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationOrganizationControllerListApiKeysV1 } from "../../funcs/organizationOrganizationControllerListApiKeysV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$organizationOrganizationControllerListApiKeysV1:
  ToolDefinition = {
    name: "list-api-keys",
    description: `List API keys

List active API keys for an organization so administrators can audit automation access and rotate credentials safely.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] = await organizationOrganizationControllerListApiKeysV1(
        client,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
