/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationOrganizationControllerGetAvailableScopesV1 } from "../../funcs/organizationOrganizationControllerGetAvailableScopesV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$organizationOrganizationControllerGetAvailableScopesV1:
  ToolDefinition = {
    name: "get-available-scopes",
    description: `List API key scopes

Retrieve available API key scopes and permissions before creating credentials for a specific compliance automation workflow.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] =
        await organizationOrganizationControllerGetAvailableScopesV1(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
