/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationOrganizationAccessControllerAutoApproveV1 } from "../../funcs/organizationOrganizationAccessControllerAutoApproveV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$organizationOrganizationAccessControllerAutoApproveV1:
  ToolDefinition = {
    name: "auto-approve",
    description:
      `Auto-approve organization access via domain or self-hosted check

Grants hasAccess on the active organization if the requesting user is an internal trycomp.ai user, the deployment is self-hosted, or the user email domain matches the organization website domain and is an active Stripe customer.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] =
        await organizationOrganizationAccessControllerAutoApproveV1(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
