/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { cloudSecurityCloudSecurityControllerGetActivityV1 } from "../../funcs/cloudSecurityCloudSecurityControllerGetActivityV1.js";
import { CloudSecurityControllerGetActivityV1Request$zodSchema } from "../../models/cloudsecuritycontrollergetactivityv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CloudSecurityControllerGetActivityV1Request$zodSchema,
};

export const tool$cloudSecurityCloudSecurityControllerGetActivityV1:
  ToolDefinition<typeof args> = {
    name: "get-activity",
    description: `List recent cloud security activity

List recent cloud security activity in Comp AI. Run AWS, Azure, and GCP cloud security scans, detect enabled services, review findings, and connect cloud posture results to compliance work.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await cloudSecurityCloudSecurityControllerGetActivityV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
