/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { devicesDevicesControllerGetAllDevicesV1 } from "../../funcs/devicesDevicesControllerGetAllDevicesV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$devicesDevicesControllerGetAllDevicesV1: ToolDefinition = {
  name: "get-all-devices",
  description: `List managed devices

List managed employee devices with endpoint compliance status, ownership, and security check results for workforce controls.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  tool: async (client, ctx) => {
    const [result] = await devicesDevicesControllerGetAllDevicesV1(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
