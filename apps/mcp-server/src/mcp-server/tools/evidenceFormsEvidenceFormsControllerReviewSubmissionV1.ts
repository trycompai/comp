/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evidenceFormsEvidenceFormsControllerReviewSubmissionV1 } from "../../funcs/evidenceFormsEvidenceFormsControllerReviewSubmissionV1.js";
import { EvidenceFormsControllerReviewSubmissionV1Request$zodSchema } from "../../models/evidenceformscontrollerreviewsubmissionv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EvidenceFormsControllerReviewSubmissionV1Request$zodSchema,
};

export const tool$evidenceFormsEvidenceFormsControllerReviewSubmissionV1:
  ToolDefinition<typeof args> = {
    name: "review-submission",
    description: `Review evidence submission

Approve or reject a submitted evidence form so task status and audit readiness reflect the latest review decision.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await evidenceFormsEvidenceFormsControllerReviewSubmissionV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
