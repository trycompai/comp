/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { findingsFindingsControllerCreateFindingV1 } from "../../funcs/findingsFindingsControllerCreateFindingV1.js";
import { CreateFindingDto$zodSchema } from "../../models/createfindingdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateFindingDto$zodSchema,
};

export const tool$findingsFindingsControllerCreateFindingV1: ToolDefinition<
  typeof args
> = {
  name: "create-finding",
  description: `Create audit finding

Create an audit finding so teams can track issue ownership, remediation activity, severity, and supporting evidence.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await findingsFindingsControllerCreateFindingV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
