/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evidenceFormsEvidenceFormsControllerUploadFileV1 } from "../../funcs/evidenceFormsEvidenceFormsControllerUploadFileV1.js";
import { EvidenceFormsControllerUploadFileV1Request$zodSchema } from "../../models/evidenceformscontrolleruploadfilev1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EvidenceFormsControllerUploadFileV1Request$zodSchema.optional(),
};

export const tool$evidenceFormsEvidenceFormsControllerUploadFileV1:
  ToolDefinition<typeof args> = {
    name: "upload-file",
    description: `Upload evidence form file

Upload evidence form file in Comp AI. Collect, review, upload, and export structured evidence submissions for compliance tasks and document requirements.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await evidenceFormsEvidenceFormsControllerUploadFileV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
