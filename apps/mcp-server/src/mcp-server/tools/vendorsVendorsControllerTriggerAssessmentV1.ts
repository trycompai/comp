/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vendorsVendorsControllerTriggerAssessmentV1 } from "../../funcs/vendorsVendorsControllerTriggerAssessmentV1.js";
import { VendorsControllerTriggerAssessmentV1Request$zodSchema } from "../../models/vendorscontrollertriggerassessmentv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VendorsControllerTriggerAssessmentV1Request$zodSchema,
};

export const tool$vendorsVendorsControllerTriggerAssessmentV1: ToolDefinition<
  typeof args
> = {
  name: "trigger-assessment",
  description: `Trigger vendor risk assessment

Trigger a vendor risk assessment so Comp AI can update third-party risk evidence and vendor security review status.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await vendorsVendorsControllerTriggerAssessmentV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
