/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deviceAgentDeviceAgentControllerDownloadWindowsAgentV1 } from "../../funcs/deviceAgentDeviceAgentControllerDownloadWindowsAgentV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$deviceAgentDeviceAgentControllerDownloadWindowsAgentV1:
  ToolDefinition = {
    name: "download-windows-agent",
    description: `Download Windows Device Agent ZIP

Downloads a ZIP package containing the Comp AI Device Agent installer for Windows, along with setup scripts and instructions. The package includes an MSI installer, setup batch script customized for the organization and user, and a README.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] =
        await deviceAgentDeviceAgentControllerDownloadWindowsAgentV1(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
