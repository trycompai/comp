/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trustPortalTrustPortalControllerListComplianceResourcesV1 } from "../../funcs/trustPortalTrustPortalControllerListComplianceResourcesV1.js";
import { ListComplianceResourcesDto$zodSchema } from "../../models/listcomplianceresourcesdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListComplianceResourcesDto$zodSchema,
};

export const tool$trustPortalTrustPortalControllerListComplianceResourcesV1:
  ToolDefinition<typeof args> = {
    name: "list-compliance-resources",
    description: `List uploaded compliance certificates for the organization

List uploaded compliance certificates for the organization in Comp AI. Configure the live Trust Center, custom domain, public overview, FAQs, compliance resources, documents, links, and vendor disclosures.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await trustPortalTrustPortalControllerListComplianceResourcesV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
