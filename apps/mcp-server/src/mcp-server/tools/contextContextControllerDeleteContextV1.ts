/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contextContextControllerDeleteContextV1 } from "../../funcs/contextContextControllerDeleteContextV1.js";
import { ContextControllerDeleteContextV1Request$zodSchema } from "../../models/contextcontrollerdeletecontextv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ContextControllerDeleteContextV1Request$zodSchema,
};

export const tool$contextContextControllerDeleteContextV1: ToolDefinition<
  typeof args
> = {
  name: "delete-context",
  description: `Delete context entry

Permanently removes a context entry from the organization. This action cannot be undone.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await contextContextControllerDeleteContextV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
