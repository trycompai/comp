/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeBaseKnowledgeBaseControllerGetDownloadUrlV1 } from "../../funcs/knowledgeBaseKnowledgeBaseControllerGetDownloadUrlV1.js";
import { KnowledgeBaseControllerGetDownloadUrlV1Request$zodSchema } from "../../models/knowledgebasecontrollergetdownloadurlv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: KnowledgeBaseControllerGetDownloadUrlV1Request$zodSchema,
};

export const tool$knowledgeBaseKnowledgeBaseControllerGetDownloadUrlV1:
  ToolDefinition<typeof args> = {
    name: "get-download-url",
    description: `Get a signed download URL for a document

Get a signed download URL for a document in Comp AI. Upload source documents, process them for retrieval, and manage reusable manual answers that power questionnaires and AI policy workflows.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await knowledgeBaseKnowledgeBaseControllerGetDownloadUrlV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
