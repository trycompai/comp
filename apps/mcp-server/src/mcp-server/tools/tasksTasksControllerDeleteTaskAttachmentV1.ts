/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksTasksControllerDeleteTaskAttachmentV1 } from "../../funcs/tasksTasksControllerDeleteTaskAttachmentV1.js";
import { TasksControllerDeleteTaskAttachmentV1Request$zodSchema } from "../../models/taskscontrollerdeletetaskattachmentv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TasksControllerDeleteTaskAttachmentV1Request$zodSchema,
};

export const tool$tasksTasksControllerDeleteTaskAttachmentV1: ToolDefinition<
  typeof args
> = {
  name: "delete-task-attachment",
  description: `Delete task attachment

Delete task attachment in Comp AI. Manage compliance task lifecycle, assignments, review approvals, evidence uploads, policy links, and activity history.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksTasksControllerDeleteTaskAttachmentV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
