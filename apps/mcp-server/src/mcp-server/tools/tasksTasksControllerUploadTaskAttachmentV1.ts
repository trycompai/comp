/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksTasksControllerUploadTaskAttachmentV1 } from "../../funcs/tasksTasksControllerUploadTaskAttachmentV1.js";
import { TasksControllerUploadTaskAttachmentV1Request$zodSchema } from "../../models/taskscontrolleruploadtaskattachmentv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TasksControllerUploadTaskAttachmentV1Request$zodSchema,
};

export const tool$tasksTasksControllerUploadTaskAttachmentV1: ToolDefinition<
  typeof args
> = {
  name: "upload-task-attachment",
  description: `Upload task evidence

Upload an evidence attachment to a task so auditors and reviewers can trace completion back to source documentation.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksTasksControllerUploadTaskAttachmentV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
