/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksTasksControllerRegenerateTaskV1 } from "../../funcs/tasksTasksControllerRegenerateTaskV1.js";
import { TasksControllerRegenerateTaskV1Request$zodSchema } from "../../models/taskscontrollerregeneratetaskv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TasksControllerRegenerateTaskV1Request$zodSchema,
};

export const tool$tasksTasksControllerRegenerateTaskV1: ToolDefinition<
  typeof args
> = {
  name: "regenerate-task",
  description: `Regenerate task from template

Regenerate task from template in Comp AI. Manage compliance task lifecycle, assignments, review approvals, evidence uploads, policy links, and activity history.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksTasksControllerRegenerateTaskV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
