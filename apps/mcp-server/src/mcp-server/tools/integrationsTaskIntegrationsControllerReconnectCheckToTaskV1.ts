/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsTaskIntegrationsControllerReconnectCheckToTaskV1 } from "../../funcs/integrationsTaskIntegrationsControllerReconnectCheckToTaskV1.js";
import { TaskIntegrationsControllerReconnectCheckToTaskV1Request$zodSchema } from "../../models/taskintegrationscontrollerreconnectchecktotaskv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TaskIntegrationsControllerReconnectCheckToTaskV1Request$zodSchema,
};

export const tool$integrationsTaskIntegrationsControllerReconnectCheckToTaskV1:
  ToolDefinition<typeof args> = {
    name: "reconnect-check-to-task",
    description: `Reconnect checks to a task

Reconnect checks to a task in Comp AI. Connect vendor systems, configure OAuth apps, run compliance checks, sync employees, manage variables, and collect automated evidence.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await integrationsTaskIntegrationsControllerReconnectCheckToTaskV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
