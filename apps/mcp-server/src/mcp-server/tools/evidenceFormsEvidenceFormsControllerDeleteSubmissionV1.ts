/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evidenceFormsEvidenceFormsControllerDeleteSubmissionV1 } from "../../funcs/evidenceFormsEvidenceFormsControllerDeleteSubmissionV1.js";
import { EvidenceFormsControllerDeleteSubmissionV1Request$zodSchema } from "../../models/evidenceformscontrollerdeletesubmissionv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EvidenceFormsControllerDeleteSubmissionV1Request$zodSchema,
};

export const tool$evidenceFormsEvidenceFormsControllerDeleteSubmissionV1:
  ToolDefinition<typeof args> = {
    name: "delete-submission",
    description: `Delete a submission

Delete a submission in Comp AI. Collect, review, upload, and export structured evidence submissions for compliance tasks and document requirements.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await evidenceFormsEvidenceFormsControllerDeleteSubmissionV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
