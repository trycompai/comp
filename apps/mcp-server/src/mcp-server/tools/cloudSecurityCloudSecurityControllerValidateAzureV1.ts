/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { cloudSecurityCloudSecurityControllerValidateAzureV1 } from "../../funcs/cloudSecurityCloudSecurityControllerValidateAzureV1.js";
import { CloudSecurityControllerValidateAzureV1Request$zodSchema } from "../../models/cloudsecuritycontrollervalidateazurev1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CloudSecurityControllerValidateAzureV1Request$zodSchema,
};

export const tool$cloudSecurityCloudSecurityControllerValidateAzureV1:
  ToolDefinition<typeof args> = {
    name: "validate-azure",
    description: `Validate Azure credentials for a connection

Validate Azure credentials for a connection in Comp AI. Run AWS, Azure, and GCP cloud security scans, detect enabled services, review findings, and connect cloud posture results to compliance work.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await cloudSecurityCloudSecurityControllerValidateAzureV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
