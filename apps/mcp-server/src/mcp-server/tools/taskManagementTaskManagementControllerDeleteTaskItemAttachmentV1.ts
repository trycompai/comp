/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taskManagementTaskManagementControllerDeleteTaskItemAttachmentV1 } from "../../funcs/taskManagementTaskManagementControllerDeleteTaskItemAttachmentV1.js";
import { TaskManagementControllerDeleteTaskItemAttachmentV1Request$zodSchema } from "../../models/taskmanagementcontrollerdeletetaskitemattachmentv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TaskManagementControllerDeleteTaskItemAttachmentV1Request$zodSchema,
};

export const tool$taskManagementTaskManagementControllerDeleteTaskItemAttachmentV1:
  ToolDefinition<typeof args> = {
    name: "delete-task-item-attachment",
    description: `Delete attachment from task item

Delete attachment from task item in Comp AI. Manage task items and attachments linked to operational entities such as risks and vendors.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await taskManagementTaskManagementControllerDeleteTaskItemAttachmentV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
