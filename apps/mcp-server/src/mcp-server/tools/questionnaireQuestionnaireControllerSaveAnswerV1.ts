/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { questionnaireQuestionnaireControllerSaveAnswerV1 } from "../../funcs/questionnaireQuestionnaireControllerSaveAnswerV1.js";
import { SaveAnswerDto$zodSchema } from "../../models/saveanswerdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SaveAnswerDto$zodSchema,
};

export const tool$questionnaireQuestionnaireControllerSaveAnswerV1:
  ToolDefinition<typeof args> = {
    name: "save-answer",
    description: `Save questionnaire answer

Save a manual or AI-generated security questionnaire answer for later review, export, and audit tracking.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await questionnaireQuestionnaireControllerSaveAnswerV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
