/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { peoplePeopleControllerReactivateMemberV1 } from "../../funcs/peoplePeopleControllerReactivateMemberV1.js";
import { PeopleControllerReactivateMemberV1Request$zodSchema } from "../../models/peoplecontrollerreactivatememberv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PeopleControllerReactivateMemberV1Request$zodSchema,
};

export const tool$peoplePeopleControllerReactivateMemberV1: ToolDefinition<
  typeof args
> = {
  name: "reactivate-member",
  description: `Reactivate a deactivated member

Reactivate a deactivated member in Comp AI. Invite and manage workforce members, training status, device compliance, email preferences, and employee evidence records.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await peoplePeopleControllerReactivateMemberV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
