/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { rolesRolesControllerListRolesV1 } from "../../funcs/rolesRolesControllerListRolesV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$rolesRolesControllerListRolesV1: ToolDefinition = {
  name: "list-roles",
  description: `List all roles

List all roles in Comp AI. Create custom roles and resolve permission sets for organization-level access control.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  tool: async (client, ctx) => {
    const [result] = await rolesRolesControllerListRolesV1(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
