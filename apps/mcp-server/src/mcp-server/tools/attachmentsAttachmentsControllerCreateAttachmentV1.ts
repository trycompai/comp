/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { attachmentsAttachmentsControllerCreateAttachmentV1 } from "../../funcs/attachmentsAttachmentsControllerCreateAttachmentV1.js";
import { CreateAttachmentDto$zodSchema } from "../../models/createattachmentdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateAttachmentDto$zodSchema,
};

export const tool$attachmentsAttachmentsControllerCreateAttachmentV1:
  ToolDefinition<typeof args> = {
    name: "create-attachment",
    description: `Upload an attachment to any supported entity

Upload a base64-encoded file and attach it to a task, vendor, risk, comment, or other supported entity type. The file is uploaded to S3 and a database record is created.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await attachmentsAttachmentsControllerCreateAttachmentV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
