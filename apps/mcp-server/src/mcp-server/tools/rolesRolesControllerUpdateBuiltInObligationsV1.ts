/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { rolesRolesControllerUpdateBuiltInObligationsV1 } from "../../funcs/rolesRolesControllerUpdateBuiltInObligationsV1.js";
import { RolesControllerUpdateBuiltInObligationsV1Request$zodSchema } from "../../models/rolescontrollerupdatebuiltinobligationsv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RolesControllerUpdateBuiltInObligationsV1Request$zodSchema,
};

export const tool$rolesRolesControllerUpdateBuiltInObligationsV1:
  ToolDefinition<typeof args> = {
    name: "update-built-in-obligations",
    description: `Update obligations for a built-in role

Override the obligations for a built-in role (e.g., turn off the compliance obligation for owners). Permissions stay sourced from the hardcoded defaults.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await rolesRolesControllerUpdateBuiltInObligationsV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
