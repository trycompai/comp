/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deviceAgentDeviceAgentControllerExchangeCodeV1 } from "../../funcs/deviceAgentDeviceAgentControllerExchangeCodeV1.js";
import { ExchangeCodeDto$zodSchema } from "../../models/exchangecodedto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ExchangeCodeDto$zodSchema,
};

export const tool$deviceAgentDeviceAgentControllerExchangeCodeV1:
  ToolDefinition<typeof args> = {
    name: "exchange-code",
    description: `Exchange an auth code for device credentials

Exchange an auth code for device credentials in Comp AI. Register employee devices, submit device compliance check-ins, download agent builds, and manage endpoint security status.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await deviceAgentDeviceAgentControllerExchangeCodeV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
