/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trustPortalTrustPortalControllerDeleteTrustDocumentV1 } from "../../funcs/trustPortalTrustPortalControllerDeleteTrustDocumentV1.js";
import { TrustPortalControllerDeleteTrustDocumentV1Request$zodSchema } from "../../models/trustportalcontrollerdeletetrustdocumentv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TrustPortalControllerDeleteTrustDocumentV1Request$zodSchema,
};

export const tool$trustPortalTrustPortalControllerDeleteTrustDocumentV1:
  ToolDefinition<typeof args> = {
    name: "delete-trust-document",
    description: `Delete (deactivate) a trust portal document

Delete (deactivate) a trust portal document in Comp AI. Configure the live Trust Center, custom domain, public overview, FAQs, compliance resources, documents, links, and vendor disclosures.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await trustPortalTrustPortalControllerDeleteTrustDocumentV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
