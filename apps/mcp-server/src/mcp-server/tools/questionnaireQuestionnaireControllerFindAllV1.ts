/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { questionnaireQuestionnaireControllerFindAllV1 } from "../../funcs/questionnaireQuestionnaireControllerFindAllV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$questionnaireQuestionnaireControllerFindAllV1:
  ToolDefinition = {
    name: "questionnaire-find-all",
    description: `List security questionnaires

List saved security questionnaires for an organization so teams can track customer reviews, answer status, and response history.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] = await questionnaireQuestionnaireControllerFindAllV1(
        client,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
