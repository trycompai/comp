/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { soaSOAControllerSubmitForApprovalV1 } from "../../funcs/soaSOAControllerSubmitForApprovalV1.js";
import { SubmitSOAForApprovalDto$zodSchema } from "../../models/submitsoaforapprovaldto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SubmitSOAForApprovalDto$zodSchema,
};

export const tool$soaSOAControllerSubmitForApprovalV1: ToolDefinition<
  typeof args
> = {
  name: "submit-for-approval",
  description: `Submit SOA document for approval

Submit SOA document for approval in Comp AI. Create, auto-fill, review, approve, and export ISO 27001 Statement of Applicability documents.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await soaSOAControllerSubmitForApprovalV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
