/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationOrganizationControllerTransferOwnershipV1 } from "../../funcs/organizationOrganizationControllerTransferOwnershipV1.js";
import { OrganizationControllerTransferOwnershipV1Request$zodSchema } from "../../models/organizationcontrollertransferownershipv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: OrganizationControllerTransferOwnershipV1Request$zodSchema.describe(
    `Transfer organization ownership to another member`,
  ),
};

export const tool$organizationOrganizationControllerTransferOwnershipV1:
  ToolDefinition<typeof args> = {
    name: "transfer-ownership",
    description: `Transfer organization ownership

Transfers organization ownership to another member. The current owner will become an admin and keep all other roles. The new owner will receive the owner role while keeping their existing roles. Only the current organization owner can.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await organizationOrganizationControllerTransferOwnershipV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
