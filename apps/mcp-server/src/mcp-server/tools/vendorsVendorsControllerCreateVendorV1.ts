/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vendorsVendorsControllerCreateVendorV1 } from "../../funcs/vendorsVendorsControllerCreateVendorV1.js";
import { CreateVendorDto$zodSchema } from "../../models/createvendordto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateVendorDto$zodSchema.describe(`Vendor creation data`),
};

export const tool$vendorsVendorsControllerCreateVendorV1: ToolDefinition<
  typeof args
> = {
  name: "create-vendor",
  description: `Create vendor

Create a vendor record so teams can track third-party risk, assessment evidence, owner, category, and compliance status.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await vendorsVendorsControllerCreateVendorV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
