/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evidenceFormsEvidenceFormsControllerExportCsvV1 } from "../../funcs/evidenceFormsEvidenceFormsControllerExportCsvV1.js";
import { EvidenceFormsControllerExportCsvV1Request$zodSchema } from "../../models/evidenceformscontrollerexportcsvv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EvidenceFormsControllerExportCsvV1Request$zodSchema,
};

export const tool$evidenceFormsEvidenceFormsControllerExportCsvV1:
  ToolDefinition<typeof args> = {
    name: "export-csv",
    description: `Export evidence submissions

Export evidence form submissions as CSV for auditor requests, offline review, or internal compliance reporting.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await evidenceFormsEvidenceFormsControllerExportCsvV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
