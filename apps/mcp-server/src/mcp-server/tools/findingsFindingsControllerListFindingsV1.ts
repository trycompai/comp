/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { findingsFindingsControllerListFindingsV1 } from "../../funcs/findingsFindingsControllerListFindingsV1.js";
import { FindingsControllerListFindingsV1Request$zodSchema } from "../../models/findingscontrollerlistfindingsv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FindingsControllerListFindingsV1Request$zodSchema.optional(),
};

export const tool$findingsFindingsControllerListFindingsV1: ToolDefinition<
  typeof args
> = {
  name: "list-findings",
  description: `List audit findings

List audit findings with status, severity, owner, history, and remediation context for compliance review workflows.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await findingsFindingsControllerListFindingsV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
