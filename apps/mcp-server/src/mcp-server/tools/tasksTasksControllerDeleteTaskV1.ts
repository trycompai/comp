/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksTasksControllerDeleteTaskV1 } from "../../funcs/tasksTasksControllerDeleteTaskV1.js";
import { TasksControllerDeleteTaskV1Request$zodSchema } from "../../models/taskscontrollerdeletetaskv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TasksControllerDeleteTaskV1Request$zodSchema,
};

export const tool$tasksTasksControllerDeleteTaskV1: ToolDefinition<
  typeof args
> = {
  name: "delete-task",
  description: `Delete a task

Delete a task in Comp AI. Manage compliance task lifecycle, assignments, review approvals, evidence uploads, policy links, and activity history.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksTasksControllerDeleteTaskV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
