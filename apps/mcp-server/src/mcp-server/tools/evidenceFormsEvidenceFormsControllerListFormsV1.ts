/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evidenceFormsEvidenceFormsControllerListFormsV1 } from "../../funcs/evidenceFormsEvidenceFormsControllerListFormsV1.js";
import { EvidenceFormsControllerListFormsV1Request$zodSchema } from "../../models/evidenceformscontrollerlistformsv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EvidenceFormsControllerListFormsV1Request$zodSchema.optional(),
};

export const tool$evidenceFormsEvidenceFormsControllerListFormsV1:
  ToolDefinition<typeof args> = {
    name: "list-forms",
    description: `List evidence forms

List structured evidence forms that collect recurring submissions for security, HR, IT, finance, and compliance workflows.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await evidenceFormsEvidenceFormsControllerListFormsV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
