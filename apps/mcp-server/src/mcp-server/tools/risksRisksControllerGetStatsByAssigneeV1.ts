/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { risksRisksControllerGetStatsByAssigneeV1 } from "../../funcs/risksRisksControllerGetStatsByAssigneeV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$risksRisksControllerGetStatsByAssigneeV1: ToolDefinition = {
  name: "get-stats-by-assignee",
  description: `Get risk statistics grouped by assignee

Get risk statistics grouped by assignee in Comp AI. Create, update, and report on organizational risks with ownership, departments, and compliance remediation status.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  tool: async (client, ctx) => {
    const [result] = await risksRisksControllerGetStatsByAssigneeV1(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
