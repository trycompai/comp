/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evidenceFormsEvidenceFormsControllerGetSubmissionV1 } from "../../funcs/evidenceFormsEvidenceFormsControllerGetSubmissionV1.js";
import { EvidenceFormsControllerGetSubmissionV1Request$zodSchema } from "../../models/evidenceformscontrollergetsubmissionv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EvidenceFormsControllerGetSubmissionV1Request$zodSchema,
};

export const tool$evidenceFormsEvidenceFormsControllerGetSubmissionV1:
  ToolDefinition<typeof args> = {
    name: "get-submission",
    description: `Get a single submission

Get a single submission in Comp AI. Collect, review, upload, and export structured evidence submissions for compliance tasks and document requirements.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await evidenceFormsEvidenceFormsControllerGetSubmissionV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
