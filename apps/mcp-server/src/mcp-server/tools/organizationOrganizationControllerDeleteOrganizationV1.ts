/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationOrganizationControllerDeleteOrganizationV1 } from "../../funcs/organizationOrganizationControllerDeleteOrganizationV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$organizationOrganizationControllerDeleteOrganizationV1:
  ToolDefinition = {
    name: "delete-organization",
    description: `Delete organization

Permanently deletes the authenticated organization. This action cannot be undone.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] =
        await organizationOrganizationControllerDeleteOrganizationV1(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
