/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { cloudSecurityCloudSecurityControllerGetRunStatusV1 } from "../../funcs/cloudSecurityCloudSecurityControllerGetRunStatusV1.js";
import { CloudSecurityControllerGetRunStatusV1Request$zodSchema } from "../../models/cloudsecuritycontrollergetrunstatusv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CloudSecurityControllerGetRunStatusV1Request$zodSchema,
};

export const tool$cloudSecurityCloudSecurityControllerGetRunStatusV1:
  ToolDefinition<typeof args> = {
    name: "get-run-status",
    description: `Get a cloud security scan run by ID

Get a cloud security scan run by ID in Comp AI. Run AWS, Azure, and GCP cloud security scans, detect enabled services, review findings, and connect cloud posture results to compliance work.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await cloudSecurityCloudSecurityControllerGetRunStatusV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
