/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsSyncControllerGetEmployeeSyncProviderV1 } from "../../funcs/integrationsSyncControllerGetEmployeeSyncProviderV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$integrationsSyncControllerGetEmployeeSyncProviderV1:
  ToolDefinition = {
    name: "get-employee-sync-provider",
    description: `Get the currently configured employee sync provider

Get the currently configured employee sync provider in Comp AI. Connect vendor systems, configure OAuth apps, run compliance checks, sync employees, manage variables, and collect automated evidence.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] =
        await integrationsSyncControllerGetEmployeeSyncProviderV1(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
