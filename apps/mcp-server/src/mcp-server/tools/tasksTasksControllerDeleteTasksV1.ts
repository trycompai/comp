/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksTasksControllerDeleteTasksV1 } from "../../funcs/tasksTasksControllerDeleteTasksV1.js";
import { TasksControllerDeleteTasksV1Request$zodSchema } from "../../models/taskscontrollerdeletetasksv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TasksControllerDeleteTasksV1Request$zodSchema,
};

export const tool$tasksTasksControllerDeleteTasksV1: ToolDefinition<
  typeof args
> = {
  name: "delete-tasks",
  description: `Delete multiple tasks

Delete multiple tasks in Comp AI. Manage compliance task lifecycle, assignments, review approvals, evidence uploads, policy links, and activity history.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksTasksControllerDeleteTasksV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
