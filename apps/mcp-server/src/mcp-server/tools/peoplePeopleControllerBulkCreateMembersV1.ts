/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { peoplePeopleControllerBulkCreateMembersV1 } from "../../funcs/peoplePeopleControllerBulkCreateMembersV1.js";
import { BulkCreatePeopleDto$zodSchema } from "../../models/bulkcreatepeopledto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: BulkCreatePeopleDto$zodSchema.describe(`Bulk member creation data`),
};

export const tool$peoplePeopleControllerBulkCreateMembersV1: ToolDefinition<
  typeof args
> = {
  name: "bulk-create-members",
  description: `Add multiple members to organization

Bulk adds multiple members to the authenticated organization. Each member must have a valid user ID that exists in the system. Members who already exist in the organization or have invalid data will be skipped with error details returned.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await peoplePeopleControllerBulkCreateMembersV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
