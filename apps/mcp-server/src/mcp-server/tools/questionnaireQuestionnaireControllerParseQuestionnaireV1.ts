/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { questionnaireQuestionnaireControllerParseQuestionnaireV1 } from "../../funcs/questionnaireQuestionnaireControllerParseQuestionnaireV1.js";
import { ParseQuestionnaireDto$zodSchema } from "../../models/parsequestionnairedto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ParseQuestionnaireDto$zodSchema,
};

export const tool$questionnaireQuestionnaireControllerParseQuestionnaireV1:
  ToolDefinition<typeof args> = {
    name: "parse-questionnaire",
    description: `Parse questionnaire content

Parse questionnaire content from a submitted payload so teams can extract security questions before generating or reviewing answers.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await questionnaireQuestionnaireControllerParseQuestionnaireV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
