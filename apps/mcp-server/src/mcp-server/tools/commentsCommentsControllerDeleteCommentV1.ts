/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commentsCommentsControllerDeleteCommentV1 } from "../../funcs/commentsCommentsControllerDeleteCommentV1.js";
import { CommentsControllerDeleteCommentV1Request$zodSchema } from "../../models/commentscontrollerdeletecommentv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CommentsControllerDeleteCommentV1Request$zodSchema,
};

export const tool$commentsCommentsControllerDeleteCommentV1: ToolDefinition<
  typeof args
> = {
  name: "delete-comment",
  description: `Delete a comment

Delete a comment in Comp AI. Create and manage collaboration comments on compliance entities such as tasks, policies, risks, vendors, and findings.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await commentsCommentsControllerDeleteCommentV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
