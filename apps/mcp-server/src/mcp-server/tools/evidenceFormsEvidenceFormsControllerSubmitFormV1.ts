/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evidenceFormsEvidenceFormsControllerSubmitFormV1 } from "../../funcs/evidenceFormsEvidenceFormsControllerSubmitFormV1.js";
import { EvidenceFormsControllerSubmitFormV1Request$zodSchema } from "../../models/evidenceformscontrollersubmitformv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EvidenceFormsControllerSubmitFormV1Request$zodSchema,
};

export const tool$evidenceFormsEvidenceFormsControllerSubmitFormV1:
  ToolDefinition<typeof args> = {
    name: "submit-form",
    description: `Submit evidence form

Submit structured evidence responses and attachments for review against a compliance task or document requirement.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await evidenceFormsEvidenceFormsControllerSubmitFormV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
