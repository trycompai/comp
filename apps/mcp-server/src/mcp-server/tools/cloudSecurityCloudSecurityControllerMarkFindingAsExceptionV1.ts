/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { cloudSecurityCloudSecurityControllerMarkFindingAsExceptionV1 } from "../../funcs/cloudSecurityCloudSecurityControllerMarkFindingAsExceptionV1.js";
import { CloudSecurityControllerMarkFindingAsExceptionV1Request$zodSchema } from "../../models/cloudsecuritycontrollermarkfindingasexceptionv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CloudSecurityControllerMarkFindingAsExceptionV1Request$zodSchema,
};

export const tool$cloudSecurityCloudSecurityControllerMarkFindingAsExceptionV1:
  ToolDefinition<typeof args> = {
    name: "mark-finding-as-exception",
    description:
      `Mark a finding as an exception so it no longer appears in the active Scan Results list

Mark a finding as an exception so it no longer appears in the active Scan Results list in Comp AI. Run AWS, Azure, and GCP cloud security scans, detect enabled services, review findings, and connect cloud posture results to compliance work.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await cloudSecurityCloudSecurityControllerMarkFindingAsExceptionV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
