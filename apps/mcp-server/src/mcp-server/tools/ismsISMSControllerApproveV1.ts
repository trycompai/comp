/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ismsISMSControllerApproveV1 } from "../../funcs/ismsISMSControllerApproveV1.js";
import { IsmsControllerApproveV1Request$zodSchema } from "../../models/ismscontrollerapprovev1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: IsmsControllerApproveV1Request$zodSchema,
};

export const tool$ismsISMSControllerApproveV1: ToolDefinition<typeof args> = {
  name: "approve",
  description: `Approve an ISMS document

Approve an ISMS document in Comp AI.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await ismsISMSControllerApproveV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
