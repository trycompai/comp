/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { cloudSecurityCloudSecurityControllerGetProvidersV1 } from "../../funcs/cloudSecurityCloudSecurityControllerGetProvidersV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$cloudSecurityCloudSecurityControllerGetProvidersV1:
  ToolDefinition = {
    name: "get-providers",
    description: `List supported cloud providers

List supported cloud providers in Comp AI. Run AWS, Azure, and GCP cloud security scans, detect enabled services, review findings, and connect cloud posture results to compliance work.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] = await cloudSecurityCloudSecurityControllerGetProvidersV1(
        client,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
