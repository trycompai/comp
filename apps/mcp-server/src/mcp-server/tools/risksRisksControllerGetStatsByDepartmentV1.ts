/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { risksRisksControllerGetStatsByDepartmentV1 } from "../../funcs/risksRisksControllerGetStatsByDepartmentV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$risksRisksControllerGetStatsByDepartmentV1: ToolDefinition = {
  name: "get-stats-by-department",
  description: `Get risk counts grouped by department

Get risk counts grouped by department in Comp AI. Create, update, and report on organizational risks with ownership, departments, and compliance remediation status.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  tool: async (client, ctx) => {
    const [result] = await risksRisksControllerGetStatsByDepartmentV1(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
