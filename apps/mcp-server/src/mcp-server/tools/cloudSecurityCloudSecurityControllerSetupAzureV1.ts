/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { cloudSecurityCloudSecurityControllerSetupAzureV1 } from "../../funcs/cloudSecurityCloudSecurityControllerSetupAzureV1.js";
import { CloudSecurityControllerSetupAzureV1Request$zodSchema } from "../../models/cloudsecuritycontrollersetupazurev1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CloudSecurityControllerSetupAzureV1Request$zodSchema,
};

export const tool$cloudSecurityCloudSecurityControllerSetupAzureV1:
  ToolDefinition<typeof args> = {
    name: "setup-azure",
    description: `Set up Azure for a connection

Set up Azure for a connection in Comp AI. Run AWS, Azure, and GCP cloud security scans, detect enabled services, review findings, and connect cloud posture results to compliance work.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await cloudSecurityCloudSecurityControllerSetupAzureV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
