/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trainingTrainingControllerGenerateCertificateV1 } from "../../funcs/trainingTrainingControllerGenerateCertificateV1.js";
import { SendTrainingCompletionDto$zodSchema } from "../../models/sendtrainingcompletiondto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SendTrainingCompletionDto$zodSchema,
};

export const tool$trainingTrainingControllerGenerateCertificateV1:
  ToolDefinition<typeof args> = {
    name: "generate-certificate",
    description: `Generate training certificate

Generate a training completion certificate that can be shared with auditors or attached as workforce security evidence.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await trainingTrainingControllerGenerateCertificateV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
