/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsConnectionsControllerResumeConnectionV1 } from "../../funcs/integrationsConnectionsControllerResumeConnectionV1.js";
import { ConnectionsControllerResumeConnectionV1Request$zodSchema } from "../../models/connectionscontrollerresumeconnectionv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ConnectionsControllerResumeConnectionV1Request$zodSchema,
};

export const tool$integrationsConnectionsControllerResumeConnectionV1:
  ToolDefinition<typeof args> = {
    name: "resume-connection",
    description: `Resume an integration connection

Resume an integration connection in Comp AI. Connect vendor systems, configure OAuth apps, run compliance checks, sync employees, manage variables, and collect automated evidence.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await integrationsConnectionsControllerResumeConnectionV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
