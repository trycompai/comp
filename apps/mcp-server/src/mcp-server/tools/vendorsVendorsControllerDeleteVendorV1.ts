/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vendorsVendorsControllerDeleteVendorV1 } from "../../funcs/vendorsVendorsControllerDeleteVendorV1.js";
import { VendorsControllerDeleteVendorV1Request$zodSchema } from "../../models/vendorscontrollerdeletevendorv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VendorsControllerDeleteVendorV1Request$zodSchema,
};

export const tool$vendorsVendorsControllerDeleteVendorV1: ToolDefinition<
  typeof args
> = {
  name: "delete-vendor",
  description: `Delete vendor

Permanently removes a vendor from the organization. This action cannot be undone.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await vendorsVendorsControllerDeleteVendorV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
