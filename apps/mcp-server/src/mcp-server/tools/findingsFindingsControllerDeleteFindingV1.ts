/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { findingsFindingsControllerDeleteFindingV1 } from "../../funcs/findingsFindingsControllerDeleteFindingV1.js";
import { FindingsControllerDeleteFindingV1Request$zodSchema } from "../../models/findingscontrollerdeletefindingv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FindingsControllerDeleteFindingV1Request$zodSchema,
};

export const tool$findingsFindingsControllerDeleteFindingV1: ToolDefinition<
  typeof args
> = {
  name: "delete-finding",
  description: `Delete a finding (auditor or platform admin only)

Delete a finding (auditor or platform admin only) in Comp AI. Create, review, update, and track audit findings, remediation activity, and finding history for an organization.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await findingsFindingsControllerDeleteFindingV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
