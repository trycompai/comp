/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { evidenceFormsEvidenceFormsControllerGetFormStatusesV1 } from "../../funcs/evidenceFormsEvidenceFormsControllerGetFormStatusesV1.js";
import { EvidenceFormsControllerGetFormStatusesV1Request$zodSchema } from "../../models/evidenceformscontrollergetformstatusesv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EvidenceFormsControllerGetFormStatusesV1Request$zodSchema.optional(),
};

export const tool$evidenceFormsEvidenceFormsControllerGetFormStatusesV1:
  ToolDefinition<typeof args> = {
    name: "get-form-statuses",
    description: `Get submission statuses for all forms

Get submission statuses for all forms in Comp AI. Collect, review, upload, and export structured evidence submissions for compliance tasks and document requirements.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await evidenceFormsEvidenceFormsControllerGetFormStatusesV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
