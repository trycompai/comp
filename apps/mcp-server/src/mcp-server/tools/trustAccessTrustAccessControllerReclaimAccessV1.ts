/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trustAccessTrustAccessControllerReclaimAccessV1 } from "../../funcs/trustAccessTrustAccessControllerReclaimAccessV1.js";
import { TrustAccessControllerReclaimAccessV1Request$zodSchema } from "../../models/trustaccesscontrollerreclaimaccessv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TrustAccessControllerReclaimAccessV1Request$zodSchema,
};

export const tool$trustAccessTrustAccessControllerReclaimAccessV1:
  ToolDefinition<typeof args> = {
    name: "reclaim-access",
    description: `Reclaim Trust Access link

Request a fresh Trust Access link for a reviewer who already has an active grant on a published Trust Center.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await trustAccessTrustAccessControllerReclaimAccessV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
