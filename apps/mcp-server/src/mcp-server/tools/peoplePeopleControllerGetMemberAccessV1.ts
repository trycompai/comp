/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { peoplePeopleControllerGetMemberAccessV1 } from "../../funcs/peoplePeopleControllerGetMemberAccessV1.js";
import { PeopleControllerGetMemberAccessV1Request$zodSchema } from "../../models/peoplecontrollergetmemberaccessv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PeopleControllerGetMemberAccessV1Request$zodSchema,
};

export const tool$peoplePeopleControllerGetMemberAccessV1: ToolDefinition<
  typeof args
> = {
  name: "get-member-access",
  description: `Member's access across connected integrations

Aggregates the latest Employee Access check results from every connected integration bound to the Employee Access task, matched to the member by email.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await peoplePeopleControllerGetMemberAccessV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
