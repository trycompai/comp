/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ismsISMSControllerEnsureSetupV1 } from "../../funcs/ismsISMSControllerEnsureSetupV1.js";
import { EnsureIsmsSetupDto$zodSchema } from "../../models/ensureismssetupdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EnsureIsmsSetupDto$zodSchema,
};

export const tool$ismsISMSControllerEnsureSetupV1: ToolDefinition<typeof args> =
  {
    name: "isms-ensure-setup",
    description: `Ensure ISMS foundational documents exist

Ensure ISMS foundational documents exist in Comp AI.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await ismsISMSControllerEnsureSetupV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
