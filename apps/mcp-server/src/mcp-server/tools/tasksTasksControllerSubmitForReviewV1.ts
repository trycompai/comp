/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksTasksControllerSubmitForReviewV1 } from "../../funcs/tasksTasksControllerSubmitForReviewV1.js";
import { TasksControllerSubmitForReviewV1Request$zodSchema } from "../../models/taskscontrollersubmitforreviewv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TasksControllerSubmitForReviewV1Request$zodSchema,
};

export const tool$tasksTasksControllerSubmitForReviewV1: ToolDefinition<
  typeof args
> = {
  name: "submit-for-review",
  description: `Submit task for review

Submit task for review in Comp AI. Manage compliance task lifecycle, assignments, review approvals, evidence uploads, policy links, and activity history.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksTasksControllerSubmitForReviewV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
