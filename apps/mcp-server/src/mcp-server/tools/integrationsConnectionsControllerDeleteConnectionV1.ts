/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsConnectionsControllerDeleteConnectionV1 } from "../../funcs/integrationsConnectionsControllerDeleteConnectionV1.js";
import { ConnectionsControllerDeleteConnectionV1Request$zodSchema } from "../../models/connectionscontrollerdeleteconnectionv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ConnectionsControllerDeleteConnectionV1Request$zodSchema,
};

export const tool$integrationsConnectionsControllerDeleteConnectionV1:
  ToolDefinition<typeof args> = {
    name: "delete-connection",
    description: `Delete an integration connection

Delete an integration connection in Comp AI. Connect vendor systems, configure OAuth apps, run compliance checks, sync employees, manage variables, and collect automated evidence.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await integrationsConnectionsControllerDeleteConnectionV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
