/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { policiesPoliciesControllerGetAllPoliciesV1 } from "../../funcs/policiesPoliciesControllerGetAllPoliciesV1.js";
import { PoliciesControllerGetAllPoliciesV1Request$zodSchema } from "../../models/policiescontrollergetallpoliciesv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PoliciesControllerGetAllPoliciesV1Request$zodSchema.optional(),
};

export const tool$policiesPoliciesControllerGetAllPoliciesV1: ToolDefinition<
  typeof args
> = {
  name: "list-policies",
  description: `List compliance policies

Lists compliance policies for the organization. Use this to find a policy by name, look up a policy ID, browse drafts, or get an overview of all policies for SOC 2, ISO 27001, HIPAA, and GDPR workflows. Returns id, name, status, department, and other metadata for each policy. Pass excludeContent=true to skip the heavy TipTap content fields — recommended when you only need to identify a policy. To read or edit a single policy in detail, fetch it by ID via get-compliance-policy.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await policiesPoliciesControllerGetAllPoliciesV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
