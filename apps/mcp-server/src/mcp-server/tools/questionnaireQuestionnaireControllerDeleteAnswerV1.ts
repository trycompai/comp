/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { questionnaireQuestionnaireControllerDeleteAnswerV1 } from "../../funcs/questionnaireQuestionnaireControllerDeleteAnswerV1.js";
import { DeleteAnswerDto$zodSchema } from "../../models/deleteanswerdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteAnswerDto$zodSchema,
};

export const tool$questionnaireQuestionnaireControllerDeleteAnswerV1:
  ToolDefinition<typeof args> = {
    name: "delete-answer",
    description: `Delete questionnaire answer

Delete a stored questionnaire answer when it should be removed from the active response set.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await questionnaireQuestionnaireControllerDeleteAnswerV1(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
