/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { trustPortalTrustPortalControllerUploadCustomFrameworkBadgeV1 } from "../../funcs/trustPortalTrustPortalControllerUploadCustomFrameworkBadgeV1.js";
import { UploadCustomFrameworkBadgeDto$zodSchema } from "../../models/uploadcustomframeworkbadgedto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UploadCustomFrameworkBadgeDto$zodSchema,
};

export const tool$trustPortalTrustPortalControllerUploadCustomFrameworkBadgeV1:
  ToolDefinition<typeof args> = {
    name: "upload-custom-framework-badge",
    description:
      `Upload or replace a custom framework's Trust Portal badge image

Stores a PNG/JPEG/WebP badge (max 256KB) in the organization assets bucket. Does not change the framework's portal visibility.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await trustPortalTrustPortalControllerUploadCustomFrameworkBadgeV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
