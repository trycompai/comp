/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeBaseKnowledgeBaseControllerListManualAnswersV1 } from "../../funcs/knowledgeBaseKnowledgeBaseControllerListManualAnswersV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$knowledgeBaseKnowledgeBaseControllerListManualAnswersV1:
  ToolDefinition = {
    name: "list-manual-answers",
    description: `List all manual answers for an organization

List all manual answers for an organization in Comp AI. Upload source documents, process them for retrieval, and manage reusable manual answers that power questionnaires and AI policy workflows.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] =
        await knowledgeBaseKnowledgeBaseControllerListManualAnswersV1(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
