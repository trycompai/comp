/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taskManagementTaskManagementControllerUploadTaskItemAttachmentV1 } from "../../funcs/taskManagementTaskManagementControllerUploadTaskItemAttachmentV1.js";
import { UploadTaskItemAttachmentDto$zodSchema } from "../../models/uploadtaskitemattachmentdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UploadTaskItemAttachmentDto$zodSchema,
};

export const tool$taskManagementTaskManagementControllerUploadTaskItemAttachmentV1:
  ToolDefinition<typeof args> = {
    name: "upload-task-item-attachment",
    description: `Upload attachment to task item

Upload a file attachment for a task item with proper S3 path structure: org_{orgId}/attachments/task-item/{entityType}/{entityId}/files.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await taskManagementTaskManagementControllerUploadTaskItemAttachmentV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
