/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { uploadsUploadsControllerCreateUploadUrlV1 } from "../../funcs/uploadsUploadsControllerCreateUploadUrlV1.js";
import { UploadsControllerCreateUploadUrlV1Request$zodSchema } from "../../models/uploadscontrollercreateuploadurlv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UploadsControllerCreateUploadUrlV1Request$zodSchema,
};

export const tool$uploadsUploadsControllerCreateUploadUrlV1: ToolDefinition<
  typeof args
> = {
  name: "create-upload-url",
  description: `Get a presigned URL to upload a file

Returns a presigned S3 URL plus the s3Key the file lands at. PUT the raw file bytes to that URL, then call the feature tool (e.g. upload-and-parse) with the s3Key instead of sending file data. Bytes never pass through the LLM.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await uploadsUploadsControllerCreateUploadUrlV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
