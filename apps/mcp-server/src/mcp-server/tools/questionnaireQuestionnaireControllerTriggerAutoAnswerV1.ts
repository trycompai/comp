/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { questionnaireQuestionnaireControllerTriggerAutoAnswerV1 } from "../../funcs/questionnaireQuestionnaireControllerTriggerAutoAnswerV1.js";
import { QuestionnaireControllerTriggerAutoAnswerV1Request$zodSchema } from "../../models/questionnairecontrollertriggerautoanswerv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: QuestionnaireControllerTriggerAutoAnswerV1Request$zodSchema,
};

export const tool$questionnaireQuestionnaireControllerTriggerAutoAnswerV1:
  ToolDefinition<typeof args> = {
    name: "generate-questionnaire-answers",
    description: `Generate answers for a questionnaire

Starts background answer generation for an already-parsed questionnaire and returns a run handle immediately. Poll GET /v1/questionnaire/:id until answeredQuestions equals totalQuestions, then read the answers from its questions.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await questionnaireQuestionnaireControllerTriggerAutoAnswerV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
