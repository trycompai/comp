/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { cloudSecurityCloudSecurityControllerGetCheckDefinitionV1 } from "../../funcs/cloudSecurityCloudSecurityControllerGetCheckDefinitionV1.js";
import { CloudSecurityControllerGetCheckDefinitionV1Request$zodSchema } from "../../models/cloudsecuritycontrollergetcheckdefinitionv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CloudSecurityControllerGetCheckDefinitionV1Request$zodSchema,
};

export const tool$cloudSecurityCloudSecurityControllerGetCheckDefinitionV1:
  ToolDefinition<typeof args> = {
    name: "get-check-definition",
    description:
      `Resolve the "About this check" description for a finding (AI-cached for AWS; provider-derived for GCP/Azure)

Resolve the "About this check" description for a finding (AI-cached for AWS; provider-derived for GCP/Azure) in Comp AI. Run AWS, Azure, and GCP cloud security scans, detect enabled services, review findings, and connect cloud posture.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await cloudSecurityCloudSecurityControllerGetCheckDefinitionV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
