/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ismsISMSControllerDriftV1 } from "../../funcs/ismsISMSControllerDriftV1.js";
import { IsmsControllerDriftV1Request$zodSchema } from "../../models/ismscontrollerdriftv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: IsmsControllerDriftV1Request$zodSchema,
};

export const tool$ismsISMSControllerDriftV1: ToolDefinition<typeof args> = {
  name: "drift",
  description: `Detect drift against the approved snapshot

Detect drift against the approved snapshot in Comp AI.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await ismsISMSControllerDriftV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
