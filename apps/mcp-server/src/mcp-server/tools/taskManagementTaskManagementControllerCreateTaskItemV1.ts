/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taskManagementTaskManagementControllerCreateTaskItemV1 } from "../../funcs/taskManagementTaskManagementControllerCreateTaskItemV1.js";
import { CreateTaskItemDto$zodSchema } from "../../models/createtaskitemdto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateTaskItemDto$zodSchema,
};

export const tool$taskManagementTaskManagementControllerCreateTaskItemV1:
  ToolDefinition<typeof args> = {
    name: "create-task-item",
    description: `Create a new task item

Create a new task item in Comp AI. Manage task items and attachments linked to operational entities such as risks and vendors.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await taskManagementTaskManagementControllerCreateTaskItemV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
