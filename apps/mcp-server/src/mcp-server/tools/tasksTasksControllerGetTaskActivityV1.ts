/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksTasksControllerGetTaskActivityV1 } from "../../funcs/tasksTasksControllerGetTaskActivityV1.js";
import { TasksControllerGetTaskActivityV1Request$zodSchema } from "../../models/taskscontrollergettaskactivityv1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TasksControllerGetTaskActivityV1Request$zodSchema,
};

export const tool$tasksTasksControllerGetTaskActivityV1: ToolDefinition<
  typeof args
> = {
  name: "get-task-activity",
  description: `Get task activity

Get task activity in Comp AI. Manage compliance task lifecycle, assignments, review approvals, evidence uploads, policy links, and activity history.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksTasksControllerGetTaskActivityV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
