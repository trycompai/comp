/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deviceAgentDeviceAgentControllerDownloadMacAgentV1 } from "../../funcs/deviceAgentDeviceAgentControllerDownloadMacAgentV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$deviceAgentDeviceAgentControllerDownloadMacAgentV1:
  ToolDefinition = {
    name: "download-mac-agent",
    description: `Download macOS Device Agent

Downloads the Comp AI Device Agent installer for macOS as a DMG file. The agent helps monitor device compliance and security policies.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    tool: async (client, ctx) => {
      const [result] = await deviceAgentDeviceAgentControllerDownloadMacAgentV1(
        client,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
