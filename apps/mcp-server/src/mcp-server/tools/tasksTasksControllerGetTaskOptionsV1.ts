/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksTasksControllerGetTaskOptionsV1 } from "../../funcs/tasksTasksControllerGetTaskOptionsV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$tasksTasksControllerGetTaskOptionsV1: ToolDefinition = {
  name: "get-task-options",
  description: `Get page options for tasks overview

Get page options for tasks overview in Comp AI. Manage compliance task lifecycle, assignments, review approvals, evidence uploads, policy links, and activity history.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  tool: async (client, ctx) => {
    const [result] = await tasksTasksControllerGetTaskOptionsV1(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
