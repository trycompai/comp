/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksTasksControllerUpdateTasksAssigneeV1 } from "../../funcs/tasksTasksControllerUpdateTasksAssigneeV1.js";
import { TasksControllerUpdateTasksAssigneeV1Request$zodSchema } from "../../models/taskscontrollerupdatetasksassigneev1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TasksControllerUpdateTasksAssigneeV1Request$zodSchema,
};

export const tool$tasksTasksControllerUpdateTasksAssigneeV1: ToolDefinition<
  typeof args
> = {
  name: "update-tasks-assignee",
  description: `Update assignee for multiple tasks

Update assignee for multiple tasks in Comp AI. Manage compliance task lifecycle, assignments, review approvals, evidence uploads, policy links, and activity history.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksTasksControllerUpdateTasksAssigneeV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
