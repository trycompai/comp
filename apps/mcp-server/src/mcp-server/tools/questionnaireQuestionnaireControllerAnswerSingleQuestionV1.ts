/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { questionnaireQuestionnaireControllerAnswerSingleQuestionV1 } from "../../funcs/questionnaireQuestionnaireControllerAnswerSingleQuestionV1.js";
import { AnswerSingleQuestionDto$zodSchema } from "../../models/answersinglequestiondto.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AnswerSingleQuestionDto$zodSchema,
};

export const tool$questionnaireQuestionnaireControllerAnswerSingleQuestionV1:
  ToolDefinition<typeof args> = {
    name: "answer-single-question",
    description: `Answer one questionnaire question

Generate an answer for one security questionnaire item using the organization evidence library and return source references for review.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await questionnaireQuestionnaireControllerAnswerSingleQuestionV1(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
