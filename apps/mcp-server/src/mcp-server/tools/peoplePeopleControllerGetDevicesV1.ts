/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { peoplePeopleControllerGetDevicesV1 } from "../../funcs/peoplePeopleControllerGetDevicesV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$peoplePeopleControllerGetDevicesV1: ToolDefinition = {
  name: "get-devices",
  description: `Get all employee devices with fleet compliance data

Get all employee devices with fleet compliance data in Comp AI. Invite and manage workforce members, training status, device compliance, email preferences, and employee evidence records.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  tool: async (client, ctx) => {
    const [result] = await peoplePeopleControllerGetDevicesV1(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
