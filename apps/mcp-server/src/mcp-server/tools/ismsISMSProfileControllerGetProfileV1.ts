/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ismsISMSProfileControllerGetProfileV1 } from "../../funcs/ismsISMSProfileControllerGetProfileV1.js";
import { IsmsProfileControllerGetProfileV1Request$zodSchema } from "../../models/ismsprofilecontrollergetprofilev1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: IsmsProfileControllerGetProfileV1Request$zodSchema,
};

export const tool$ismsISMSProfileControllerGetProfileV1: ToolDefinition<
  typeof args
> = {
  name: "get-profile",
  description: `Get the ISMS wizard profile, defaults and members

Get the ISMS wizard profile, defaults and members in Comp AI.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await ismsISMSProfileControllerGetProfileV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
