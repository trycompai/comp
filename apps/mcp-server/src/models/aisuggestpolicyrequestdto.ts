/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export const RoleEnum = {
  User: "user",
  Assistant: "assistant",
} as const;
export type RoleEnum = ClosedEnum<typeof RoleEnum>;

export const RoleEnum$zodSchema = z.enum([
  "user",
  "assistant",
]);

export type ChatHistory = {
  role?: RoleEnum | undefined;
  content?: string | undefined;
};

export const ChatHistory$zodSchema: z.ZodType<ChatHistory> = z.object({
  content: z.string().optional(),
  role: RoleEnum$zodSchema.optional(),
});

export type AISuggestPolicyRequestDto = {
  instructions: string;
  chatHistory?: Array<ChatHistory> | undefined;
};

export const AISuggestPolicyRequestDto$zodSchema: z.ZodType<
  AISuggestPolicyRequestDto
> = z.object({
  chatHistory: z.array(z.lazy(() => ChatHistory$zodSchema)).optional().describe(
    "Chat history for context (array of messages with role and content)",
  ),
  instructions: z.string().describe(
    "User instructions about what changes to make to the policy",
  ),
});
