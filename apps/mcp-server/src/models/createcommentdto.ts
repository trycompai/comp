/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";
import {
  UploadAttachmentDto,
  UploadAttachmentDto$zodSchema,
} from "./uploadattachmentdto.js";

/**
 * Type of entity being commented on
 */
export const CreateCommentDtoEntityType = {
  Task: "task",
  Vendor: "vendor",
  Risk: "risk",
  Policy: "policy",
  Finding: "finding",
} as const;
/**
 * Type of entity being commented on
 */
export type CreateCommentDtoEntityType = ClosedEnum<
  typeof CreateCommentDtoEntityType
>;

export const CreateCommentDtoEntityType$zodSchema = z.enum([
  "task",
  "vendor",
  "risk",
  "policy",
  "finding",
]).describe("Type of entity being commented on");

export type CreateCommentDto = {
  content: string;
  entityId: string;
  entityType: CreateCommentDtoEntityType;
  contextUrl?: string | undefined;
  attachments?: Array<UploadAttachmentDto> | undefined;
  userId?: string | undefined;
};

export const CreateCommentDto$zodSchema: z.ZodType<CreateCommentDto> = z.object(
  {
    attachments: z.array(UploadAttachmentDto$zodSchema).optional().describe(
      "Optional attachments to include with the comment",
    ),
    content: z.string().describe("Content of the comment"),
    contextUrl: z.string().optional().describe(
      "Optional URL of the page where the comment was created, used for deep-linking in notifications",
    ),
    entityId: z.string().describe("ID of the entity to comment on"),
    entityType: CreateCommentDtoEntityType$zodSchema.describe(
      "Type of entity being commented on",
    ),
    userId: z.string().optional().describe(
      "User ID of the comment author (required for API key auth, ignored for JWT auth)",
    ),
  },
);
