/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

export type PolicyPdfUploadUrlResponseDto = {
  uploadUrl: string;
  s3Key: string;
  expiresIn: number;
};

export const PolicyPdfUploadUrlResponseDto$zodSchema: z.ZodType<
  PolicyPdfUploadUrlResponseDto
> = z.object({
  expiresIn: z.number().describe("Seconds until the presigned URL expires."),
  s3Key: z.string().describe(
    "The S3 key the file will land at. Pass this back to the confirm endpoint after a successful upload.",
  ),
  uploadUrl: z.string().describe(
    "Presigned S3 URL. PUT the raw file bytes to this URL with header `Content-Type: application/pdf`. No auth headers required — the signature is in the URL.",
  ),
});
