/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import {
  DomainVerificationDto,
  DomainVerificationDto$zodSchema,
} from "./domainverificationdto.js";

/**
 * Whether Vercel's /v6/domains/{d}/config call reports the domain as misconfigured. Null when Vercel could not be reached.
 */
export type Misconfigured = {};

export const Misconfigured$zodSchema: z.ZodType<Misconfigured> = z.object({})
  .describe(
    "Whether Vercel's /v6/domains/{d}/config call reports the domain as misconfigured. Null when Vercel could not be reached.",
  );

export type DomainStatusResponseDto = {
  domain: string;
  verified: boolean;
  verification?: Array<DomainVerificationDto> | undefined;
  cnameTarget?: string | undefined;
  misconfigured?: Misconfigured | null | undefined;
};

export const DomainStatusResponseDto$zodSchema: z.ZodType<
  DomainStatusResponseDto
> = z.object({
  cnameTarget: z.string().optional().describe(
    "The recommended CNAME target for this domain from Vercel",
  ),
  domain: z.string().describe("The domain name"),
  misconfigured: z.lazy(() => Misconfigured$zodSchema).nullable().optional()
    .describe(
      "Whether Vercel's /v6/domains/{d}/config call reports the domain as misconfigured. Null when Vercel could not be reached.",
    ),
  verification: z.array(DomainVerificationDto$zodSchema).optional().describe(
    "Verification records for the domain",
  ),
  verified: z.boolean().describe("Whether the domain is verified"),
});
