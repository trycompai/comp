/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

export type ConfirmPolicyPdfUploadedDto = {
  s3Key: string;
  versionId?: string | undefined;
};

export const ConfirmPolicyPdfUploadedDto$zodSchema: z.ZodType<
  ConfirmPolicyPdfUploadedDto
> = z.object({
  s3Key: z.string().describe(
    "The exact s3Key returned by the upload-url endpoint.",
  ),
  versionId: z.string().optional().describe(
    "Optional version ID — must match the versionId passed to the upload-url endpoint. Omit if attaching at the policy level.",
  ),
});
