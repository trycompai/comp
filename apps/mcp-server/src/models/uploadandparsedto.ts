/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

export type UploadAndParseDto = {
  organizationId: string;
  fileName: string;
  fileType: string;
  s3Key?: string | undefined;
};

export const UploadAndParseDto$zodSchema: z.ZodType<UploadAndParseDto> = z
  .object({
    fileName: z.string().describe("Name of the questionnaire file."),
    fileType: z.string().describe(
      "MIME type of the file (PDF, image, XLSX, CSV, TXT).",
    ),
    organizationId: z.string().describe(
      "Organization ID (set automatically from auth context).",
    ),
    s3Key: z.string().optional().describe(
      "The questionnaire file to parse. First call create-upload-url with purpose=questionnaire to get a presigned URL and s3Key, PUT the raw file bytes to that URL, then pass the returned s3Key here. This is the only way to supply a file — never send file contents inline.",
    ),
  });
