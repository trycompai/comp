/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

export type UploadDocumentDto = {
  organizationId: string;
  fileName: string;
  fileType: string;
  s3Key?: string | undefined;
  description?: string | undefined;
};

export const UploadDocumentDto$zodSchema: z.ZodType<UploadDocumentDto> = z
  .object({
    description: z.string().optional().describe("Optional description"),
    fileName: z.string().describe("File name"),
    fileType: z.string().describe("MIME type of the file"),
    organizationId: z.string().describe(
      "Organization ID that owns the document",
    ),
    s3Key: z.string().optional().describe(
      "Key of a file already uploaded via /v1/uploads/presign (purpose=document). The server fetches the bytes from storage — no base64 needed. Provide exactly one of fileData or s3Key.",
    ),
  });
