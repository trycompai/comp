export const borders = {
  // Centralized border styles (width + style). Color should come from semantic tokens (e.g. `border`).
  none: { value: 'none' },
  subtle: { value: '1px solid' },
  strong: { value: '2px solid' },
} as const;
