generator client {
  provider            = "prisma-client"
  output              = "../src/generated/prisma"
  previewFeatures     = ["postgresqlExtensions"]
  importFileExtension = ""
}

datasource db {
  provider   = "postgresql"
  extensions = [pgcrypto]
}
